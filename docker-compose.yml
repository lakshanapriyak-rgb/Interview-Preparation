services:
  app:
    build: .
    ports:
      - '8080:8080'
    depends_on:
      - prometheus

  prometheus:
    image: prom/prometheus
    ports:
      - '9090:9090'
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
